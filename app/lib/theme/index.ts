/**
 * Theme management module.
 *
 * Handles color scheme persistence and provides theme constants for the app.
 */

// Constants
export { NAV_THEME, THEME } from "@/lib/theme/constants"
// React context
export { ThemeProvider, useTheme } from "@/lib/theme/context"
// Types
export type { ColorScheme } from "@/lib/theme/types"
